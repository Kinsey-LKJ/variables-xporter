---
title: CSS Variables
---

import { Callout } from 'nextra/components'
import { Cards } from 'nextra/components'
import { Zap, RefreshCw, Palette, Type, Columns3, BookOpenCheck,Box,Ruler } from 'lucide-react'

# CSS Variables

When choosing the CSS Variables export mode, Variables Xporter will generate a CSS file containing all CSS variable definitions. This approach is simpler and more flexible, suitable for projects not using Tailwind CSS.

## Features

### 1. Simple and Intuitive
Directly outputs standard CSS custom properties:

```css
:root {
  --colors-primary: #c03939;
  --colors-primary-foreground: #ffffff;
  --font-size-heading-1: 2.5rem;
  --line-height-heading-1: 1.2;
}
```

### 2. Flexible Usage
These variables can be used in any CSS rule:

```css
.button {
  background-color: var(--colors-primary);
  color: var(--colors-primary-foreground);
  font-size: var(--font-size-heading-1);
  line-height: var(--line-height-heading-1);
}
```

### 3. Multi-mode Support
Through **variable tracing** and the **Single Outlet Principle**, Variables Xporter can implement complex multi-mode variable exports.

```css
:root {
  --colors-primary: var(--colors-primary-light);
}

.dark {
  --colors-primary: var(--colors-primary-dark);
}
```

  <Cards.Card
    icon={<Columns3 />}
    title="Multi-mode Support"
    href="/docs/features/multi-mode"
    className="mt-4"
  />

## Variable Organization Guide

Compared to [Tailwind CSS](/docs/export-modes/tailwind), the CSS Variables export mode has lower requirements for variable organization, but it's still recommended to follow good organization principles to:

- Improve variable maintainability
- Maintain design system consistency
- Facilitate team collaboration
- Prepare for possible framework migration in the future

### Basic Principles
You can first understand the basic principles of variable organization, which will help you make better decisions regarding variable naming, grouping, and hierarchy.

  <Cards.Card
    icon={<BookOpenCheck  />}
    title="Basic Principles"
    href="/docs/organizing-your-variables/principles"
    className="mt-4"
  />

### Variable Types
Then, you can further organize your variables based on different variable types.

<Cards>
  <Cards.Card
    icon={<Palette />}
    title="Color System"
    href="/docs/organizing-your-variables/colors"
  />
  <Cards.Card
    icon={<Type />}
    title="Typography System"
    href="/docs/organizing-your-variables/typography"
  />
  <Cards.Card
    icon={<Ruler />}
    title="Spacing and Size System"
    href="/docs/organizing-your-variables/spacing"
  />
  <Cards.Card
    icon={<Box/>}
    title="Other Tokens"
    href="/docs/organizing-your-variables/other-tokens"
  />
  <Cards.Card
    icon={<Columns3 />}
    title="Multi-mode"
    href="/docs/organizing-your-variables/multi-mode"
  />
</Cards>
