import Example from "../../app/components/example/example"
import { getDictionary } from "../../app/_dictionaries/get-dictionary"

export async function ExampleWrapper({ params }) {
  const dictionary = await getDictionary(params.lang)
  console.log(dictionary,"example")
  return <Example params={params} dictionary={dictionary} />
}

# Exemple

<div className="mt-10 text-foreground">
<ExampleWrapper params={props.params} />
</div>



